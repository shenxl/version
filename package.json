{
  "name": "workbrench-api",
  "version": "1.0.0",
  "description": "基于Python的Cloudflare Worker项目",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "git push -u origin main"
  },
  "devDependencies": {
    "wrangler": "^4.12.0"
  }
} 